[project]
name = "apify-image-similarity-api-example"
version = "0.1.0"
description = "Python example for the image comparison API on Apify: score image similarity, find duplicates, and compare two images."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apify-client>=3,<4",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
